[project]
name = "gemma-shared"
version = "0.1.0"
description = "Shared Gemma 4 loader for the voice app"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["gemma_shared"]
